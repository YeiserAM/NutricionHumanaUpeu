import { DatosUsuariosDirective } from './datos-usuarios.directive';

describe('DatosUsuariosDirective', () => {
  it('should create an instance', () => {
    const directive = new DatosUsuariosDirective();
    expect(directive).toBeTruthy();
  });
});
